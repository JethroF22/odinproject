<h1>All tags</h1>

<ul id="tags">
  <% @tags.each do |tag| %>
  <li>
    <%= link_to tag.name, tag_path(tag), class: "tag_name" %>
    <%= link_to "(delete)", tag_path(@tags), method: :delete, data: {confirm: "Really delete the tag?"} %><br />
  </li>
  <% end %>
</ul>
